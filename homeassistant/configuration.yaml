# Loads default set of integrations. Do not remove.
default_config:

# go2rtc integration — point to the Frigate LXC's go2rtc instance
# (192.168.86.85:1984) instead of the local HA-managed one.
go2rtc:
  url: http://192.168.86.85:1984

# SSO via Authentik (hass-oidc-auth custom component, installed via HACS).
# Public client (PKCE) — matches hass-oidc-auth's own recommendation for a
# home setup: no client_secret to store/leak, redirect URL + PKCE is enough.
# Authentik provider: pk=12, client_id=home-assistant, app slug=home-assistant.
auth_oidc:
  client_id: "home-assistant"
  discovery_url: "https://auth.chi.lab.amd-e.com/application/o/home-assistant/.well-known/openid-configuration"

# NOTE: lb-01's trusted-proxy config for the homeassistant.chi.lab.amd-e.com
# vhost (X-Forwarded-For/-Proto/-Host trust so client IPs in the
# frontend/logbook and auth flows are correct — lb-01 reaches HA via its LAN
# NIC 192.168.86.86, not the SDN IP 172.16.0.30) now lives in HA's
# storage-backed network config (Settings > System > Network), NOT here.
#
# As of HA core 2026.8 stable, the `http:` YAML block is a one-time import
# only — the "Fix trusted proxies type when HTTP config is loaded from
# storage" bugfix (2026.8 changelog) landed and storage-based config now
# works correctly. On 2026-08-06 the pending YAML `http:` block was
# confirmed migrated and *promoted* to stable via the http/config
# websocket API (use_x_forwarded_for: true, trusted_proxies:
# ["192.168.86.86/32"]) and verified live through the reverse proxy
# (200 from https://homeassistant.chi.lab.amd-e.com/api/). Do not re-add an
# `http:` block here — HA repairs will flag "yaml_still_present_after
# migration" and it stops working entirely in 2027.2.0. Manage trusted
# proxies going forward via Settings > System > Network in the UI, or via
# the same websocket API (http/config, http/config/configure,
# http/config/promote).

# Prometheus Metrics
prometheus:

# Load frontend themes from the themes folder
logger:
  default: warning
  logs:
    homeassistant.components.automation: debug
    homeassistant.core: debug
    homeassistant.helpers.script: debug

frontend:
  themes: !include_dir_merge_named themes


# Packages for manual configuration
homeassistant:
  packages:
    manual_automations:
      automation: !include_dir_merge_list automations/

template: !include templates.yaml
sensor: !include sensors.yaml
automation: !include automations.yaml
script: !include scripts.yaml
script custom: !include_dir_merge_list scripts/
scene: !include scenes.yaml
scene custom: !include_dir_merge_list scenes/
input_datetime: !include input_datetime.yaml
input_number: !include input_number.yaml

input_text:
  living_room_active_scene:
    name: "Living Room Active Scene"
    initial: "none"

input_boolean:
  # Tracks whether the Frigate NVR watchdog has an active unresolved
  # alert. persistent_notification entities are NOT real state-machine
  # objects on this HA instance (states() on them always returns
  # "unknown" — confirmed 2026-07-28), so they can't be used to gate
  # "only send recovery notice if we actually alerted" logic. This
  # input_boolean is a real, queryable substitute for that purpose.
  frigate_watchdog_alerted:
    name: "Frigate Watchdog Alerted"
    initial: false

timer:
  garage_lights_motion_timeout:
    name: "Garage Lights Motion Timeout"
    duration: "00:15:00"
  exhaust_fan_post_hvac:
    name: "Exhaust Fan Post-HVAC Countdown"
    duration: "00:05:00"
