---
- name: Reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart grafana-ack-bot
  ansible.builtin.systemd:
    name: grafana-ack-bot
    state: restarted

- name: Save iptables rules
  ansible.builtin.command: netfilter-persistent save
  changed_when: true
