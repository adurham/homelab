# Managed by ansible — see roles/smartctl_exporter/tasks/main.yml.
[Unit]
Description=Dump SMART attributes to Prometheus textfile
After=local-fs.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/smartctl-textfile.sh
