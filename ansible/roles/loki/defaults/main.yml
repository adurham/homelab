---
# Renovate tracks https://github.com/grafana/loki releases. Verify the
# release notes between current and target before bumping — schema_config
# and compactor keys have shifted across 3.x.
loki_version: "3.7.1"
loki_port: 3100
loki_retention_period: "336h"  # 14 days
loki_storage_dir: "/var/lib/loki"
loki_user: "loki"
loki_group: "loki"
