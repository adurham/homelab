---
- name: Restart Hermes Gateway
  ansible.builtin.systemd:
    name: hermes-gateway.service
    state: restarted
    daemon_reload: true

- name: Reload Systemd
  ansible.builtin.systemd:
    daemon_reload: true
