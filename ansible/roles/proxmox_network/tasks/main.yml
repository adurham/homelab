---
- name: Deploy network configuration
  ansible.builtin.template:
    src: interfaces.j2
    dest: /etc/network/interfaces
    owner: root
    group: root
    mode: '0644'
  notify: Restart networking

- name: Reload networking immediately (if safe)
  ansible.builtin.meta: flush_handlers
